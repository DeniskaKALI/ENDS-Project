# ТАБЛИЦА ТРАССИРОВКИ ТРЕБОВАНИЙ ПРОЕКТА

| ID бизнес-требования | Описание бизнес-требования | ID системного требования | Описание системного требования | Приоритет |
|:---:|:---|:---:|:---|:---:|
| **BR-01** | «Аутентификация и безопасность доступа»<br>Пользователь должен иметь возможность зарегистрироваться и безопасно войти в систему, чтобы данные его гардероба были защищены. | SR-01.1 | Реализация экрана регистрации и входа (`Kotlin` / `Compose`). | Высокий |
| **BR-01** | «Аутентификация и безопасность доступа»<br>Пользователь должен иметь возможность зарегистрироваться и безопасно войти в систему, чтобы данные его гардероба были защищены. | SR-01.2 | Разработка `REST API` эндпоинтов `/auth/register` и `/auth/login` на `Spring Boot`. | Высокий |
| **BR-01** | «Аутентификация и безопасность доступа»<br>Пользователь должен иметь возможность зарегистрироваться и безопасно войти в систему, чтобы данные его гардероба были защищены. | SR-01.3 | Генерация и валидация `JWT-токенов` (`Spring Security`) для сессий. | Высокий |
| **BR-02** | «Оцифровка гардероба (CRUD вещей)»<br>Пользователь должен иметь возможность добавлять фото и описание вещей, чтобы создать цифровой каталог. | SR-02.1 | Реализация формы добавления вещи с выбором из галереи/камеры. | Высокий |
| **BR-02** | «Оцифровка гардероба (CRUD вещей)»<br>Пользователь должен иметь возможность добавлять фото и описание вещей, чтобы создать цифровой каталог. | SR-02.2 | Создание сущности `Item` и `Repository` в `Spring Data JPA`. | Высокий |
| **BR-02** | «Оцифровка гардероба (CRUD вещей)»<br>Пользователь должен иметь возможность добавлять фото и описание вещей, чтобы создать цифровой каталог. | SR-02.3 | Реализация CRUD-операций через `Retrofit` (клиент) и `Controllers` (сервер). | Высокий |
| **BR-03** | «Поиск и фильтрация»<br>Пользователь должен быстро находить нужные вещи по категориям или сезонам. | SR-03.1 | Реализация UI-фильтров на клиенте (`Dropdown` / `Chips`). | Средний |
| **BR-03** | «Поиск и фильтрация»<br>Пользователь должен быстро находить нужные вещи по категориям или сезонам. | SR-03.2 | Разработка эндпоинта `/api/items/search` с поддержкой `query`-параметров. | Средний |
| **BR-04** | «Создание образов (Конструктор)»<br>Пользователь должен собирать комплекты одежды, распределяя вещи по категориям (слотам). | SR-04.1 | Реализация визуального конструктора с фиксированными слотами (UI). | Высокий |
| **BR-04** | «Создание образов (Конструктор)»<br>Пользователь должен собирать комплекты одежды, распределяя вещи по категориям (слотам). | SR-04.2 | Создание сущности `Outfit` и связующей таблицы `OutfitSlot` в БД `PostgreSQL`. | Высокий |
| **BR-04** | «Создание образов (Конструктор)»<br>Пользователь должен собирать комплекты одежды, распределяя вещи по категориям (слотам). | SR-04.3 | Валидация на сервере: проверка соответствия категории вещи типу слота. | Высокий |
| **BR-05** | «Офлайн-режим и синхронизация»<br>Приложение должно работать без интернета, сохраняя данные локально и синхронизируя их позже. | SR-05.1 | Интеграция `Room Database` для локального хранения (`Local Cache`). | Высокий |
| **BR-05** | «Офлайн-режим и синхронизация»<br>Приложение должно работать без интернета, сохраняя данные локально и синхронизируя их позже. | SR-05.2 | Реализация `WorkManager` для фоновой синхронизации данных при появлении сети. | Высокий |
| **BR-05** | «Офлайн-режим и синхронизация»<br>Приложение должно работать без интернета, сохраняя данные локально и синхронизируя их позже. | SR-05.3 | Логика разрешения конфликтов (`Server wins` или `Local wins`) при синхронизации. | Средний |
| **BR-06** | «Персонализация и настройки»<br>Пользователь должен иметь возможность менять тему приложения и управлять профилем. | SR-06.1 | Реализация переключения темы (`Dark/Light Mode`) через `Material Theme`. | Низкий |
| **BR-06** | «Персонализация и настройки»<br>Пользователь должен иметь возможность менять тему приложения и управлять профилем. | SR-06.2 | Реализация эндпоинтов `/api/user/profile` для обновления данных (имя, аватар). | Средний |
| **BR-07** | «Архитектурная надежность (PCMEF)»<br>Система должна быть модульной и легко тестируемой. | SR-07.1 | Разделение кода на слои: `Presentation`, `State`, `API Client`, `Local Cache` (Client) и `Control`, `Mediator`, `Entity` (Server). | Высокий |
| **BR-07** | «Архитектурная надежность (PCMEF)»<br>Система должна быть модульной и легко тестируемой. | SR-07.2 | Покрытие модульными тестами серверной части (покрытие >40%). | Средний |
